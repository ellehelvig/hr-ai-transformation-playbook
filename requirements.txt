# Dependencies for 09-evals/run-evals.py, 05-notebooks/*.ipynb, and CI validation steps.
# Pinned to minimum tested versions, not exact versions, so security patches
# still flow through automatically.
requests>=2.31.0
PyYAML>=6.0
nbformat>=5.9.0
nbclient>=0.9.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
ipykernel>=6.25.0

# Claims registry verification. pdfminer.six extracts text from primary sources
# published as PDF, which is how most US state legislatures publish enacted acts.
# reportlab builds the PDF fixtures in scripts/test_verify_claim_sources.py, so the
# extraction logic can be tested without fetching a government website.
pdfminer.six>=20231228
reportlab>=4.0.0
